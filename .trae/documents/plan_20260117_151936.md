1. **真实房间号转换功能**：

   * 修改`src/router.js`，在parseUrl函数中添加真实房间号获取逻辑

   * 使用fetch调用斗鱼API获取真实房间号

   * 处理异步操作，确保在获取到真实房间号后再继续执行

2. **自动礼物数据更新功能**：

   * 在`src/packages/Monitor/pages/index.vue`中添加updateGiftData函数

   * 实现定期更新逻辑（每8小时）

   * 使用fetch获取最新礼物数据

   * 更新allGiftData对象

3. **错误边界处理**：

   * 在`src/App.vue`中添加错误捕获逻辑

   * 使用Vue 3的errorCaptured钩子

   * 显示错误提示信息

